<h1 class="text-center mt-4">Carrinho de Compras</h1>

<div class="container px-4 px-lg-5 mt-5">
                <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
                    <!-- Product 1-->
                    
                   
                        <table class="table">
                            <th>Foto</th>
                            <th>Nome do produto</th>
                            <th>Valor</th>
                            <th>Valor com Desconto</th>
                            <th>Quantidade</th>
                            <th>Subtotal</th>
                            <th>Ações</th>

                                    <% @cart.each do |c| %>
                                    <tr>
                                        <td><%= no_image_check(c, :cart_avatar) %></td>
                                        <td><%= c.name %></td>
                                        <td><%= formataPreco(c.price) %></td>
                                        <td><%= formataPreco(cart_price(c)) %></td>
                                        <td>
                                            <table>
                                                <tr>
                                                <td><%= button_to "-", reduzir_path(c), method: :post, class: "btn btn-primary col-md-3" %></td>
                                                <td><input type="text" value="<%= cart_quantity(c.id) %>" maxsize="1"style="width: 30px;" readonly></td>
                                                <td><%= button_to "+", aumentar_path(c), method: :post, class: "btn btn-primary col-md-3" %></td>
                                                </tr>
                                            </table>
                                            <div class="row">
                                        </td>
                                        <td><%= formataPreco(cart_price(c) * cart_quantity(c.id)) %></td>
                                        <td><%= button_to "", remover_path(c.id), class: "btn btn-danger bi bi-trash" %></td>
                                            </div>
                                    </tr>
                                    <% end %>
                                    <tr class="mt-5">
                                        <td colspan="3" class="fw-bold text-success">Descontos</td>
                                        <td class="fw-bold text-success">-
                                            <%= formataPreco(cart_sum_descont) %></td>
                                    </tr>
                                    <tr>
                                        <td colspan="5" class="fw-bold">Total</td>
                                        <td class="fw-bold">
                                            <%= formataPreco(cart_sum) %></td>
                                        <td><%= button_to "Finalizar Compra", orders_path, class: "btn btn-success" %></td>
                                    </tr>
                                
                        </table>

                        
                                
                </div>
                